{
  "name": "proxjs",
  "version": "0.0.2",
  "description": "a collection of useful rxjs creator functions, operators and others",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prepublish": "npm run lint && npm run format && npm run build",
    "build": "tsc",
    "format": "prettier --write \"src/**/*.ts\"",
    "lint": "tslint -p tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Lars Holdaas",
  "license": "ISC",
  "dependencies": {
    "rxjs": "^6.5.4"
  },
  "devDependencies": {
    "prettier": "^2.0.2",
    "tslint": "^6.1.1",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^3.8.3"
  }
}
